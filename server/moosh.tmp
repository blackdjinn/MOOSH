with NetConnect;
with Logger; use Logger;
with B32Integer;

with Ada.Text_IO;
package b32_IO is new Ada.Text_IO.Integer_IO(b32U);

procedure moosh is
begin
   Log("MOOSH running.");
   NetConnect.Testr;
   Log(B32Integer.Format(135));
   Put(B32Integer.Charval('3'));
   Put(B32Integer.Charval('C'));
   Put(B32Integer.Charval('X'));
   Log("MOOSH closing.");
end moosh;
